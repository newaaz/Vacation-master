<h2>Profile: <%= @employee.email %></h2>

<% @employee.vacations.each do |vacation| %>
  <%= link_to "Vacation request № #{vacation.id}", vacation %>
  
  <%= "from #{vacation.start_date} to #{vacation.end_date}" %>

  <br>
<% end %>
